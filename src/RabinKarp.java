public class RabinKarp {

    public static void main(String[] args) throws Exception{



    }

    /**
     * Computes (a^e) % mod using fast exponentiation.
     *
     * e >>= 1 means: shift bits of e to the right by 1 (divide e by 2).
     *   - Example: if e = 13 (binary 1101), e >>= 1 makes it 6 (binary 110).
     *   - We use this to repeatedly halve the exponent.
     */
    private static long modPow(long a, long e, long mod) {
        return 0;
    }
}
